// Self-hosted single instance — see plan decision to hardcode backend URL.
// Point this at your Rails server (use your machine's LAN IP for physical devices,
// 10.0.2.2 for Android emulator, localhost for iOS simulator).
export const API_BASE_URL = 'http://localhost:3000';
